apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-s3-tls
  namespace: minio
spec:
  secretName: minio-s3-tls
  dnsNames:
  - minio-s3.home.arpa
  issuerRef:
    kind: ClusterIssuer
    name: local-ca
