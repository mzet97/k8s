apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-tls
  namespace: kube-system
spec:
  secretName: dashboard-tls
  dnsNames:
  - dashboard.home.arpa
  issuerRef:
    kind: ClusterIssuer
    name: local-ca
